import React from 'react'

const Chart = () => {
  return <div>chart</div>
}

const domContainer = document.querySelector('#parent-container')
const root = ReactDOM.createRoot(domContainer)
root.render(e(Chart))
export default Chart
